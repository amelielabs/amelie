#

open E0 "uuid": "00000000-0000-0000-0000-000000000001", "listen": [{ "host": "127.0.0.1", "port": 3485 }], "backends": 3;
connect E0 S0 127.0.0.1:3485;

# test: default checkpoint
select show('state').lsn;
select show('state').checkpoint;

# test: checkpoint (no data)
checkpoint;
select show('state').lsn;
select show('state').checkpoint;

# test: create table
create table test (id i64 primary key);
select show('state').lsn;

# test: checkpoint
checkpoint;
select show('state').lsn;
select show('state').checkpoint;

# test: restart/recover
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;
select show('state').checkpoint;
select * from test;

# test: insert
insert into test values (0), (1), (2);

# test: restart/recover (checkpoint + wal)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

select show('state').lsn;
select show('state').checkpoint;
select * from test;

# test: checkpoint
checkpoint;
select show('state').lsn;
select show('state').checkpoint;

# test: restart/recover (checkpoint)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

select show('state').lsn;
select show('state').checkpoint;
select * from test;

# test: create clone
create clone a of test;
insert into a values (3), (4);
select * from a;

create clone b of test;
insert into b values (5), (6);
insert into b values (7), (8);
select * from b;

select timeline from show clones verbose;

# test: restart/recover (checkpoint + wal)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

select show('state').lsn;
select show('state').checkpoint;
select timeline from show clones verbose;
select * from a;
select * from b;
select * from test;

# test: checkpoint
checkpoint;
select show('state').lsn;
select show('state').checkpoint;

# test: restart/recover (checkpoint)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

select show('state').lsn;
select show('state').checkpoint;
select timeline from show clones verbose;

select * from a;
select * from b;
select * from test;

# test: create function
create function reverse(array json) return json
begin
	declare rev json := [];
	for it in (array) do
		rev := rev::push(it);
	end;
	return rev;
end;
show functions verbose;
select [1,2,3]::reverse;

# test: restart/recover (checkpoint + wal)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

select show('state').lsn;
select show('state').checkpoint;
show functions verbose;
select [1,2,3]::reverse;

# test: checkpoint
checkpoint;
select show('state').lsn;
select show('state').checkpoint;

# test: restart/recover (checkpoint)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

select show('state').lsn;
select show('state').checkpoint;
show functions verbose;
select [1,2,3]::reverse;

# test: create or replace function
create or replace function reverse(array json) return json
begin
	declare rev json := [];
	for it in (array) do
		rev := rev::push(-it::int);
	end;
	return rev;
end;
show functions verbose;
select [1,2,3]::reverse;

# test: restart/recover (checkpoint + wal)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

select show('state').lsn;
select show('state').checkpoint;
show functions verbose;
select [1,2,3]::reverse;

# test: checkpoint
checkpoint;
select show('state').lsn;
select show('state').checkpoint;

# test: restart/recover (checkpoint)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

select show('state').lsn;
select show('state').checkpoint;
show functions verbose;
select [1,2,3]::reverse;

# test: create channel
create channel test_top;
show channels;

# test: restart/recover (checkpoint + wal)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

select show('state').lsn;
select show('state').checkpoint;
show channels;

# test: checkpoint
checkpoint;
select show('state').lsn;
select show('state').checkpoint;
show channels;

# test: restart/recover (checkpoint)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

select show('state').lsn;
select show('state').checkpoint;
show channels;

# test: create user
create user test;
select show('state').lsn;

# test: restart/recover (checkpoint + wal)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

select show('state').lsn;
select show('state').checkpoint;
select show('user', 'test').name;

# test: checkpoint
checkpoint;
select show('state').lsn;
select show('state').checkpoint;

# test: restart/recover (checkpoint)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

select show('state').lsn;
select show('state').checkpoint;
select show('user', 'test').name;

# test: truncate
drop table test cascade;

create table test (id int primary key);
insert into test values (1), (2);
select * from test;

truncate test;
select * from test;

# test: checkpoint and restart
checkpoint;
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select * from test;

# test: truncate (after restart)
insert into test values (1), (2);
select * from test;

# test: checkpoint and restart
checkpoint;
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

select * from test;
truncate test;
select * from test;

# test: drop column
drop table test cascade;

create table test (id int primary key, old_col text, keep_col int);
insert into test values (1, 'gone', 10), (2, 'gone2', 20);

select show('state').checkpoint;

alter table test drop column old_col;

select show('table', 'test', null, "verbose").columns;
select old_col from test order by id;

# test: checkpoint and restart
checkpoint;

disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

select show('state').checkpoint;

select show('table', 'test', null, "verbose").columns;
select old_col from test order by id;
select * from test order by id;
drop table test;

# test: limits
show rels;

create user x;
alter user x set limit channels = 0;
alter user amelie set limit channels = 0;

# test: restart/recover (checkpoint)
checkpoint;

disconnect S0;
close E0;
open E0;
connect E0 S0 x@127.0.0.1:3485;

show limits;
show rels;

create channel test_top2;

disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;

show limits;
drop user x cascade;

disconnect S0;
close E0;
