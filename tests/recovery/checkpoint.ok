open E0 "uuid": "00000000-0000-0000-0000-000000000001", "listen": [{ "host": "127.0.0.1", "port": 3485 }], "backends": 3;
connect E0 S0 127.0.0.1:3485;
# test: default checkpoint
select show('state').lsn;

lsn
───
1
select show('state').checkpoint;

checkpoint
──────────
1
# test: checkpoint (no data)
checkpoint;
select show('state').lsn;

lsn
───
1
select show('state').checkpoint;

checkpoint
──────────
1
# test: create table
create table test (id serial primary key);
select show('state').lsn;

lsn
───
2
# test: checkpoint
checkpoint;
select show('state').lsn;

lsn
───
2
select show('state').checkpoint;

checkpoint
──────────
2
# test: restart/recover
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
2
select show('state').checkpoint;

checkpoint
──────────
2
select * from test;

id
──
# test: insert
insert into test () values (), (), ();
select identity_of("test");

identity_of
───────────
3
# test: restart/recover (checkpoint + wal)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
3
select show('state').checkpoint;

checkpoint
──────────
2
select identity_of("test");

identity_of
───────────
3
select * from test;

id
──
0
1
2
# test: checkpoint
checkpoint;
select show('state').lsn;

lsn
───
3
select show('state').checkpoint;

checkpoint
──────────
3
# test: restart/recover (checkpoint)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
3
select show('state').checkpoint;

checkpoint
──────────
3
select identity_of("test");

identity_of
───────────
3
select * from test;

id
──
0
1
2
# test: create clone
create clone a of test;
insert into a () values (), ();
select * from a;

id
──
0
1
2
3
4
create clone b of test;
insert into b () values (), ();
insert into b () values (), ();
select * from b;

id
──
0
1
6
7
8
2
5
select timeline from show clones verbose;

timeline
────────
{
  "timeline": 1,
  "timeline_max": 0
}
{
  "timeline": 2,
  "timeline_max": 0
}
# test: restart/recover (checkpoint + wal)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
8
select show('state').checkpoint;

checkpoint
──────────
3
select timeline from show clones verbose;

timeline
────────
{
  "timeline": 1,
  "timeline_max": 0
}
{
  "timeline": 2,
  "timeline_max": 0
}
select * from a;

id
──
0
1
2
3
4
select * from b;

id
──
0
1
6
7
8
2
5
select * from test;

id
──
0
1
2
# test: checkpoint
checkpoint;
select show('state').lsn;

lsn
───
8
select show('state').checkpoint;

checkpoint
──────────
8
# test: restart/recover (checkpoint)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
8
select show('state').checkpoint;

checkpoint
──────────
8
select timeline from show clones verbose;

timeline
────────
{
  "timeline": 1,
  "timeline_max": 0
}
{
  "timeline": 2,
  "timeline_max": 0
}
select * from a;

id
──
0
1
2
3
4
select * from b;

id
──
0
1
6
7
8
2
5
select * from test;

id
──
0
1
2
# test: create function
create function reverse(array json) return json
begin
	declare rev json := [];
	for it in (array) do
		rev := rev::push(it);
	end;
	return rev;
end;
show functions verbose;

functions
─────────
{
  "user": "amelie",
  "name": "reverse",
  "description": "",
  "text": "begin\n\tdeclare rev json := [];\n\tfor it in (array) do\n\t\trev := rev::push(it);\n\tend;\n\treturn rev;\nend",
  "type": 9,
  "args": [{
    "name": "array",
    "type": 9,
    "size": 0,
    "size_flat": 0,
    "dropped": false,
    "constraints": []
  }],
  "returning": [],
  "grants": []
}
select [1,2,3]::reverse;

col1
────
[3, 2, 1]
# test: restart/recover (checkpoint + wal)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
9
select show('state').checkpoint;

checkpoint
──────────
8
show functions verbose;

functions
─────────
{
  "user": "amelie",
  "name": "reverse",
  "description": "",
  "text": "begin\n\tdeclare rev json := [];\n\tfor it in (array) do\n\t\trev := rev::push(it);\n\tend;\n\treturn rev;\nend",
  "type": 9,
  "args": [{
    "name": "array",
    "type": 9,
    "size": 0,
    "size_flat": 0,
    "dropped": false,
    "constraints": []
  }],
  "returning": [],
  "grants": []
}
select [1,2,3]::reverse;

col1
────
[3, 2, 1]
# test: checkpoint
checkpoint;
select show('state').lsn;

lsn
───
9
select show('state').checkpoint;

checkpoint
──────────
9
# test: restart/recover (checkpoint)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
9
select show('state').checkpoint;

checkpoint
──────────
9
show functions verbose;

functions
─────────
{
  "user": "amelie",
  "name": "reverse",
  "description": "",
  "text": "begin\n\tdeclare rev json := [];\n\tfor it in (array) do\n\t\trev := rev::push(it);\n\tend;\n\treturn rev;\nend",
  "type": 9,
  "args": [{
    "name": "array",
    "type": 9,
    "size": 0,
    "size_flat": 0,
    "dropped": false,
    "constraints": []
  }],
  "returning": [],
  "grants": []
}
select [1,2,3]::reverse;

col1
────
[3, 2, 1]
# test: create or replace function
create or replace function reverse(array json) return json
begin
	declare rev json := [];
	for it in (array) do
		rev := rev::push(-it::int);
	end;
	return rev;
end;
show functions verbose;

functions
─────────
{
  "user": "amelie",
  "name": "reverse",
  "description": "",
  "text": "begin\n\tdeclare rev json := [];\n\tfor it in (array) do\n\t\trev := rev::push(-it::int);\n\tend;\n\treturn rev;\nend",
  "type": 9,
  "args": [{
    "name": "array",
    "type": 9,
    "size": 0,
    "size_flat": 0,
    "dropped": false,
    "constraints": []
  }],
  "returning": [],
  "grants": []
}
select [1,2,3]::reverse;

col1
────
[-3, -2, -1]
# test: restart/recover (checkpoint + wal)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
10
select show('state').checkpoint;

checkpoint
──────────
9
show functions verbose;

functions
─────────
{
  "user": "amelie",
  "name": "reverse",
  "description": "",
  "text": "begin\n\tdeclare rev json := [];\n\tfor it in (array) do\n\t\trev := rev::push(-it::int);\n\tend;\n\treturn rev;\nend",
  "type": 9,
  "args": [{
    "name": "array",
    "type": 9,
    "size": 0,
    "size_flat": 0,
    "dropped": false,
    "constraints": []
  }],
  "returning": [],
  "grants": []
}
select [1,2,3]::reverse;

col1
────
[-3, -2, -1]
# test: checkpoint
checkpoint;
select show('state').lsn;

lsn
───
10
select show('state').checkpoint;

checkpoint
──────────
10
# test: restart/recover (checkpoint)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
10
select show('state').checkpoint;

checkpoint
──────────
10
show functions verbose;

functions
─────────
{
  "user": "amelie",
  "name": "reverse",
  "description": "",
  "text": "begin\n\tdeclare rev json := [];\n\tfor it in (array) do\n\t\trev := rev::push(-it::int);\n\tend;\n\treturn rev;\nend",
  "type": 9,
  "args": [{
    "name": "array",
    "type": 9,
    "size": 0,
    "size_flat": 0,
    "dropped": false,
    "constraints": []
  }],
  "returning": [],
  "grants": []
}
select [1,2,3]::reverse;

col1
────
[-3, -2, -1]
# test: create topic
create topic test_top;
show topics;

topics
──────
{
  "user": "amelie",
  "name": "test_top",
  "description": ""
}
# test: restart/recover (checkpoint + wal)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
11
select show('state').checkpoint;

checkpoint
──────────
10
show topics;

topics
──────
{
  "user": "amelie",
  "name": "test_top",
  "description": ""
}
# test: checkpoint
checkpoint;
select show('state').lsn;

lsn
───
11
select show('state').checkpoint;

checkpoint
──────────
11
show topics;

topics
──────
{
  "user": "amelie",
  "name": "test_top",
  "description": ""
}
# test: restart/recover (checkpoint)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
11
select show('state').checkpoint;

checkpoint
──────────
11
show topics;

topics
──────
{
  "user": "amelie",
  "name": "test_top",
  "description": ""
}
# test: create subscription
create subscription top_sub on test_top;
show subs;

subs
────
{
  "user": "amelie",
  "name": "top_sub",
  "description": ""
}
publish into test_top 123;
publish into test_top 1,2,3;
publish into test_top;
select * from top_sub;

lsn  lsn_op  cmd      row
────────────────────────────
13   0       publish  123
14   0       publish  1
14   1       publish  2
14   2       publish  3
15   0       publish  null
# test: restart/recover (checkpoint + wal)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
15
select show('state').checkpoint;

checkpoint
──────────
11
select * from top_sub;

lsn  lsn_op  cmd      row
────────────────────────────
13   0       publish  123
14   0       publish  1
14   1       publish  2
14   2       publish  3
15   0       publish  null
# test: checkpoint
checkpoint;
select show('state').lsn;

lsn
───
15
select show('state').checkpoint;

checkpoint
──────────
15
select * from top_sub;

lsn  lsn_op  cmd      row
────────────────────────────
13   0       publish  123
14   0       publish  1
14   1       publish  2
14   2       publish  3
15   0       publish  null
# test: restart/recover (checkpoint)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
15
select show('state').checkpoint;

checkpoint
──────────
15
select * from top_sub;

lsn  lsn_op  cmd      row
────────────────────────────
13   0       publish  123
14   0       publish  1
14   1       publish  2
14   2       publish  3
15   0       publish  null
# test: create user
create user test;
select show('state').lsn;

lsn
───
16
# test: restart/recover (checkpoint + wal)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
16
select show('state').checkpoint;

checkpoint
──────────
15
select show('user', 'test').name;

name
────
"test"
select identity_of("test");

identity_of
───────────
9
# test: checkpoint
checkpoint;
select show('state').lsn;

lsn
───
16
select show('state').checkpoint;

checkpoint
──────────
16
# test: restart/recover (checkpoint)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').lsn;

lsn
───
16
select show('state').checkpoint;

checkpoint
──────────
16
select show('user', 'test').name;

name
────
"test"
select identity_of("test");

identity_of
───────────
9
# test: truncate
drop table test cascade;
create table test (id int primary key);
insert into test values (1), (2);
select * from test;

id
──
1
2
truncate test;
select * from test;

id
──
# test: checkpoint and restart
checkpoint;
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select * from test;

id
──
# test: truncate (after restart)
insert into test values (1), (2);
select * from test;

id
──
1
2
# test: checkpoint and restart
checkpoint;
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select * from test;

id
──
1
2
truncate test;
select * from test;

id
──
# test: drop column
drop table test cascade;
create table test (id int primary key, old_col text, keep_col int);
insert into test values (1, 'gone', 10), (2, 'gone2', 20);
select show('state').checkpoint;

checkpoint
──────────
21
alter table test drop column old_col;
select show('table', 'test', true).columns;

columns
───────
[{
  "name": "id",
  "type": 2,
  "size": 4,
  "size_flat": 0,
  "dropped": false,
  "constraints": [["not_null", true]]
}, {
  "name": "old_col",
  "type": 8,
  "size": 0,
  "size_flat": 0,
  "dropped": true,
  "constraints": []
}, {
  "name": "keep_col",
  "type": 2,
  "size": 4,
  "size_flat": 0,
  "dropped": false,
  "constraints": []
}]
select old_col from test order by id;
error: select ❰old_col❱ ⟵ column not found
# test: checkpoint and restart
checkpoint;
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select show('state').checkpoint;

checkpoint
──────────
26
select show('table', 'test', true).columns;

columns
───────
[{
  "name": "id",
  "type": 2,
  "size": 4,
  "size_flat": 0,
  "dropped": false,
  "constraints": [["not_null", true]]
}, {
  "name": "old_col",
  "type": 8,
  "size": 0,
  "size_flat": 0,
  "dropped": true,
  "constraints": []
}, {
  "name": "keep_col",
  "type": 2,
  "size": 4,
  "size_flat": 0,
  "dropped": false,
  "constraints": []
}]
select old_col from test order by id;
error: select ❰old_col❱ ⟵ column not found
select * from test order by id;

id  keep_col
──────────────
1   10
2   20
drop table test;
# test: subscription (multi-row)
create table test (id int primary key, data int);
create subscription test_sub on test;
insert into test values (1, 10), (2, 20), (3, 30);
select * from test_sub;

lsn  lsn_op  cmd    row
───────────────────────────────────────────
30   0       write  {"id": 1, "data": 10}
30   1       write  {"id": 2, "data": 20}
30   2       write  {"id": 3, "data": 30}
# test: checkpoint and restart
checkpoint;
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select * from test_sub;

lsn  lsn_op  cmd    row
───────────────────────────────────────────
30   0       write  {"id": 1, "data": 10}
30   1       write  {"id": 2, "data": 20}
30   2       write  {"id": 3, "data": 30}
select * from test order by id;

id  data
──────────
1   10
2   20
3   30
drop subscription test_sub;
drop table test;
# test: subscription (ack)
create table test (id int primary key, data int);
create subscription test_sub on test;
insert into test values (1, 10), (2, 20), (3, 30);
acknowledge test_sub to 34, 0;
select * from test_sub;

lsn  lsn_op  cmd    row
───────────────────────────────────────────
35   0       write  {"id": 1, "data": 10}
35   1       write  {"id": 2, "data": 20}
35   2       write  {"id": 3, "data": 30}
# test: checkpoint and restart
checkpoint;
select * from test_sub;

lsn  lsn_op  cmd    row
───────────────────────────────────────────
35   0       write  {"id": 1, "data": 10}
35   1       write  {"id": 2, "data": 20}
35   2       write  {"id": 3, "data": 30}
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select lsn, lsn_op from show subscriptions verbose;

lsn  lsn_op
─────────────
12   0
34   0
select * from test_sub;

lsn  lsn_op  cmd    row
───────────────────────────────────────────
35   0       write  {"id": 1, "data": 10}
35   1       write  {"id": 2, "data": 20}
35   2       write  {"id": 3, "data": 30}
select * from test order by id;

id  data
──────────
1   10
2   20
3   30
drop table test;
error: table 'test': depends on subscription 'amelie.test_sub'
drop table test cascade;
# test: subscription (drop source)
create table test (id int primary key);
create subscription test_sub on test;
drop table test;
error: table 'test': depends on subscription 'amelie.test_sub'
drop table test cascade;
checkpoint;
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
show subs;

subs
────
{
  "user": "amelie",
  "name": "top_sub",
  "description": ""
}
show tables;

tables
──────
# test: subscription (source alter)
create table test (id int primary key, data int);
create subscription test_sub on test;
insert into test values (1, 10);
select * from test_sub;

lsn  lsn_op  cmd    row
───────────────────────────────────────────
42   0       write  {"id": 1, "data": 10}
alter table test rename column data to data_new;
error: subscription 'amelie.test_sub' depends on table 'test'
select * from test_sub;

lsn  lsn_op  cmd    row
───────────────────────────────────────────
42   0       write  {"id": 1, "data": 10}
checkpoint;
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select * from test_sub;

lsn  lsn_op  cmd    row
───────────────────────────────────────────
42   0       write  {"id": 1, "data": 10}
select * from test order by id;

id  data
──────────
1   10
drop table test cascade;
# test: subscription (post checkpoint cdc)
create table test (id int primary key, data int);
create subscription test_sub on test;
checkpoint;
insert into test values (1, 0);
insert into test values (2, 0);
select * from test_sub;

lsn  lsn_op  cmd    row
──────────────────────────────────────────
46   0       write  {"id": 1, "data": 0}
47   0       write  {"id": 2, "data": 0}
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select * from test;

id  data
──────────
1   0
2   0
select * from test_sub;

lsn  lsn_op  cmd    row
──────────────────────────────────────────
46   0       write  {"id": 1, "data": 0}
47   0       write  {"id": 2, "data": 0}
drop table test cascade;
# test: subscription (clone)
create table main (id int primary key, data int) partitions 1;
create clone a of main;
create clone b of main;
create subscription sub_a on a;
create subscription sub_b on b;
begin;
insert into a values (1, 10);
insert into b values (2, 20);
end;
select * from sub_a;

lsn  lsn_op  cmd    row
───────────────────────────────────────────
54   0       write  {"id": 1, "data": 10}
select * from sub_b;

lsn  lsn_op  cmd    row
───────────────────────────────────────────
54   1       write  {"id": 2, "data": 20}
# test: restart/recover (wal)
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select * from sub_a;

lsn  lsn_op  cmd    row
───────────────────────────────────────────
54   0       write  {"id": 1, "data": 10}
select * from sub_b;

lsn  lsn_op  cmd    row
───────────────────────────────────────────
54   1       write  {"id": 2, "data": 20}
select * from a order by id;

id  data
──────────
1   10
select * from b order by id;

id  data
──────────
2   20
# test: restart/recover (checkpoint)
checkpoint;
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select * from sub_a;

lsn  lsn_op  cmd    row
───────────────────────────────────────────
54   0       write  {"id": 1, "data": 10}
select * from sub_b;

lsn  lsn_op  cmd    row
───────────────────────────────────────────
54   1       write  {"id": 2, "data": 20}
select * from a order by id;

id  data
──────────
1   10
select * from b order by id;

id  data
──────────
2   20
drop table main cascade;
disconnect S0;
close E0;
