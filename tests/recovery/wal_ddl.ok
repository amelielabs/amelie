open E0 "uuid": "00000000-0000-0000-0000-000000000000", "listen": [{ "host": "127.0.0.1", "port": 3485 }], "backends": 3;
connect E0 S0 127.0.0.1:3485;
# test: create table
create table test (id serial primary key, data int default 0);
select show('state').lsn;

lsn
───
1
select identity_of("test");

identity_of
───────────
0
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
1
select identity_of("test");

identity_of
───────────
0
# test: alter table rename
alter table test rename to test2;
select show('state').lsn;

lsn
───
2
select identity_of("test");
error: table 'test': not exists
select identity_of("test2");

identity_of
───────────
0
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
2
select identity_of("test");
error: table 'test': not exists
select identity_of("test2");

identity_of
───────────
0
# test: alter table column add
insert into test2() values (), (), ();
select * from test2;

id  data
──────────
2   0
0   0
1   0
select show('state').lsn;

lsn
───
3
select identity_of("test2");

identity_of
───────────
3
alter table test2 add column obj json;
select * from test2;

id  data  obj
────────────────
2   0     null
0   0     null
1   0     null
select obj from test2;

obj
───
null
null
null
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
4
select identity_of("test2");

identity_of
───────────
3
select * from test2;

id  data  obj
────────────────
2   0     null
0   0     null
1   0     null
# test: alter table column rename
alter table test2 rename column data to abc;
select * from test2;

id  abc  obj
───────────────
2   0    null
0   0    null
1   0    null
select data from test2;
error: select ❰data❱ ⟵ column not found
select abc from test2;

abc
───
0
0
0
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
5
select identity_of("test2");

identity_of
───────────
3
select data from test2;
error: select ❰data❱ ⟵ column not found
select abc from test2;

abc
───
0
0
0
# test: alter table set default
alter table test2 set column abc default 123;
select show('table', 'test2', true).columns;

columns
───────
[{
  "name": "id",
  "type": 2,
  "type_size": 8,
  "dropped": false,
  "constraints": [["not_null", true], ["as_identity", 1]]
}, {
  "name": "abc",
  "type": 2,
  "type_size": 4,
  "dropped": false,
  "constraints": [["default", true]]
}, {
  "name": "obj",
  "type": 5,
  "type_size": 0,
  "dropped": false,
  "constraints": []
}]
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
6
select show('table', 'test2', true).columns;

columns
───────
[{
  "name": "id",
  "type": 2,
  "type_size": 8,
  "dropped": false,
  "constraints": [["not_null", true], ["as_identity", 1]]
}, {
  "name": "abc",
  "type": 2,
  "type_size": 4,
  "dropped": false,
  "constraints": [["default", true]]
}, {
  "name": "obj",
  "type": 5,
  "type_size": 0,
  "dropped": false,
  "constraints": []
}]
# test: alter table unset default
alter table test2 unset column abc default;
select show('table', 'test2', true).columns;

columns
───────
[{
  "name": "id",
  "type": 2,
  "type_size": 8,
  "dropped": false,
  "constraints": [["not_null", true], ["as_identity", 1]]
}, {
  "name": "abc",
  "type": 2,
  "type_size": 4,
  "dropped": false,
  "constraints": []
}, {
  "name": "obj",
  "type": 5,
  "type_size": 0,
  "dropped": false,
  "constraints": []
}]
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
7
select show('table', 'test2', true).columns;

columns
───────
[{
  "name": "id",
  "type": 2,
  "type_size": 8,
  "dropped": false,
  "constraints": [["not_null", true], ["as_identity", 1]]
}, {
  "name": "abc",
  "type": 2,
  "type_size": 4,
  "dropped": false,
  "constraints": []
}, {
  "name": "obj",
  "type": 5,
  "type_size": 0,
  "dropped": false,
  "constraints": []
}]
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
7
select show('table', 'test2', true).columns;

columns
───────
[{
  "name": "id",
  "type": 2,
  "type_size": 8,
  "dropped": false,
  "constraints": [["not_null", true], ["as_identity", 1]]
}, {
  "name": "abc",
  "type": 2,
  "type_size": 4,
  "dropped": false,
  "constraints": []
}, {
  "name": "obj",
  "type": 5,
  "type_size": 0,
  "dropped": false,
  "constraints": []
}]
# test: alter table column drop
alter table test2 drop column abc;
select abc from test2;
error: select ❰abc❱ ⟵ column not found
select * from test2;

id  obj
──────────
2   null
0   null
1   null
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
8
select identity_of("test2");

identity_of
───────────
3
select * from test2;

id  obj
──────────
2   null
0   null
1   null
# test: create index
create index i on test2 (id);
select * from test2 use index (i);

id  obj
──────────
2   null
0   null
1   null
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
9
select identity_of("test2");

identity_of
───────────
3
select * from test2 use index (i);

id  obj
──────────
2   null
0   null
1   null
# test: alter index rename
alter index i on test2 rename to i2;
select show('state').lsn;

lsn
───
10
select * from test2 use index (i2);

id  obj
──────────
2   null
0   null
1   null
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
10
select identity_of("test2");

identity_of
───────────
3
select * from test2 use index (i2);

id  obj
──────────
2   null
0   null
1   null
# test: drop index
drop index i2 on test2;
select * from test2 use index (i);
error: select * from test2 use index (❰i❱ ⟵ index not found
select * from test2 use index (i2);
error: select * from test2 use index (❰i2❱ ⟵ index not found
select * from test2;

id  obj
──────────
2   null
0   null
1   null
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
11
select identity_of("test2");

identity_of
───────────
3
select * from test2 use index (i);
error: select * from test2 use index (❰i❱ ⟵ index not found
select * from test2 use index (i2);
error: select * from test2 use index (❰i2❱ ⟵ index not found
select * from test2;

id  obj
──────────
2   null
0   null
1   null
# test: create clone
create clone a of test2;
insert into a values (3, {});
select * from a;

id  obj
──────────
3   {}
2   null
0   null
1   null
create clone b of test2;
insert into b values (4, {});
insert into b values (5, []);
select * from b;

id  obj
──────────
4   {}
5   []
2   null
0   null
1   null
select snapshot from show clones verbose;

snapshot
────────
{
  "id": 1,
  "snapshot": 2,
  "snapshot_max": 2
}
{
  "id": 2,
  "snapshot": 4,
  "snapshot_max": 4
}
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
16
select snapshot from show clones verbose;

snapshot
────────
{
  "id": 1,
  "snapshot": 2,
  "snapshot_max": 2
}
{
  "id": 2,
  "snapshot": 4,
  "snapshot_max": 4
}
select * from a;

id  obj
──────────
3   {}
2   null
0   null
1   null
select * from b;

id  obj
──────────
4   {}
5   []
2   null
0   null
1   null
select * from test2;

id  obj
──────────
2   null
0   null
1   null
# test: drop clone
drop clone b;
drop clone a;
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
18
show clones;

clones
──────
# test: truncate
truncate test2;
select * from test2;

id  obj
─────────
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
19
select identity_of("test2");

identity_of
───────────
6
select * from test2;

id  obj
─────────
# test: drop table
drop table test2;
select show('state').lsn;

lsn
───
20
select identity_of("test2");
error: table 'test2': not exists
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
20
select * from test2;
error: select * from ❰test2❱ ⟵ relation not found
show tables;

tables
──────
# test: create topic
create topic top;
show topics;

topics
──────
{
  "user": "amelie",
  "name": "top"
}
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
21
show topics;

topics
──────
{
  "user": "amelie",
  "name": "top"
}
# test: create subscription
create subscription top_sub on top;
publish to top 123;
publish to top 1,2,3;
publish to top;
select * from top_sub;

lsn  lsn_op  cmd      row
────────────────────────────
23   0       publish  123
24   0       publish  1
24   1       publish  2
24   2       publish  3
25   0       publish  null
show subs;

subs
────
{
  "user": "amelie",
  "name": "top_sub"
}
create table test_ (id int primary key);
create clone test_clone of test_;
create clone test_clone2 of test_;
create subscription test_sub on test_;
create subscription test_clone_sub on test_clone;
create subscription test_clone2_sub on test_clone2;
insert into test_ values (1);
insert into test_clone values (2);
insert into test_clone2 values (3);
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
34
show subs;

subs
────
{
  "user": "amelie",
  "name": "top_sub"
}
{
  "user": "amelie",
  "name": "test_sub"
}
{
  "user": "amelie",
  "name": "test_clone_sub"
}
{
  "user": "amelie",
  "name": "test_clone2_sub"
}
select * from top_sub;

lsn  lsn_op  cmd      row
────────────────────────────
23   0       publish  123
24   0       publish  1
24   1       publish  2
24   2       publish  3
25   0       publish  null
select * from test_;

id
──
1
select * from test_clone;

id
──
2
select * from test_clone2;

id
──
3
select * from test_sub;

lsn  lsn_op  cmd    row
───────────────────────────────
32   0       write  {"id": 1}
select * from test_clone_sub;

lsn  lsn_op  cmd    row
───────────────────────────────
33   0       write  {"id": 2}
select * from test_clone2_sub;

lsn  lsn_op  cmd    row
───────────────────────────────
34   0       write  {"id": 3}
# test: drop subscription
drop subscription test_sub;
drop subscription test_clone_sub;
drop subscription test_clone2_sub;
drop table test_ cascade;
drop subscription top_sub;
show subs;

subs
────
# test: drop topic
drop topic top;
show topics;

topics
──────
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
40
show topics;

topics
──────
# test: create topic (multi-stmt)
create topic a;
create topic b;
create subscription sa on a;
create subscription sb on b;
begin
  publish to a 1;
  publish to b 2;
  publish to b 3,4,5;
end;
select * from sa;

lsn  lsn_op  cmd      row
───────────────────────────
45   0       publish  1
select * from sb;

lsn  lsn_op  cmd      row
───────────────────────────
45   1       publish  2
45   2       publish  3
45   3       publish  4
45   4       publish  5
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
select * from sa;

lsn  lsn_op  cmd      row
───────────────────────────
45   0       publish  1
select * from sb;

lsn  lsn_op  cmd      row
───────────────────────────
45   1       publish  2
45   2       publish  3
45   3       publish  4
45   4       publish  5
drop subscription sa;
drop subscription sb;
drop topic a;
drop topic b;
# test: validate state
select show('state').lsn;

lsn
───
49
show subs;

subs
────
# test: create user
create user test;
select show('user', 'test').name;

name
────
"test"
select show('state').lsn;

lsn
───
50
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
50
select show('user', 'test').name;

name
────
"test"
# test: alter user rename
alter user test rename to test2;
select show('user', 'test').name;

name
────
null
select show('user', 'test2').name;

name
────
"test2"
select show('state').lsn;

lsn
───
51
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
51
select show('user', 'test2').name;

name
────
"test2"
# test: drop user
drop user test2;
select show('state').lsn;

lsn
───
52
select show('user', 'test2').name;

name
────
null
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
52
select show('user', 'test').name;

name
────
null
select show('user', 'test2').name;

name
────
null
# test: create function
create function reverse(array json) return json
begin
	declare rev json := [];
	for it in (array) do
		rev := rev::push(it);
	end;
	return rev;
end;
select show('state').lsn;

lsn
───
53
show functions verbose;

functions
─────────
{
  "user": "amelie",
  "name": "reverse",
  "text": "begin\n\tdeclare rev json := [];\n\tfor it in (array) do\n\t\trev := rev::push(it);\n\tend;\n\treturn rev;\nend",
  "type": 5,
  "args": [{
    "name": "array",
    "type": 5,
    "type_size": 0,
    "dropped": false,
    "constraints": []
  }],
  "returning": [],
  "grants": []
}
select [1,2,3]::reverse;

col1
────
[3, 2, 1]
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
53
show functions verbose;

functions
─────────
{
  "user": "amelie",
  "name": "reverse",
  "text": "begin\n\tdeclare rev json := [];\n\tfor it in (array) do\n\t\trev := rev::push(it);\n\tend;\n\treturn rev;\nend",
  "type": 5,
  "args": [{
    "name": "array",
    "type": 5,
    "type_size": 0,
    "dropped": false,
    "constraints": []
  }],
  "returning": [],
  "grants": []
}
select [1,2,3]::reverse;

col1
────
[3, 2, 1]
# test: create or replace function
create or replace function reverse(array json) return json
begin
	declare rev json := [];
	for it in (array) do
		rev := rev::push(-it);
	end;
	return rev;
end;
error: begin
	declare rev json := [];
	for it in (array) do
		rev := rev::push(-❰it❱ ⟵ unsupported operation -json
select [1,2,3]::reverse;

col1
────
[3, 2, 1]
create or replace function reverse(array json) return json
begin
	declare rev json := [];
	for it in (array) do
		rev := rev::push(-it::int);
	end;
	return rev;
end;
select show('state').lsn;

lsn
───
54
show functions verbose;

functions
─────────
{
  "user": "amelie",
  "name": "reverse",
  "text": "begin\n\tdeclare rev json := [];\n\tfor it in (array) do\n\t\trev := rev::push(-it::int);\n\tend;\n\treturn rev;\nend",
  "type": 5,
  "args": [{
    "name": "array",
    "type": 5,
    "type_size": 0,
    "dropped": false,
    "constraints": []
  }],
  "returning": [],
  "grants": []
}
select [1,2,3]::reverse;

col1
────
[-3, -2, -1]
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
54
show functions verbose;

functions
─────────
{
  "user": "amelie",
  "name": "reverse",
  "text": "begin\n\tdeclare rev json := [];\n\tfor it in (array) do\n\t\trev := rev::push(-it::int);\n\tend;\n\treturn rev;\nend",
  "type": 5,
  "args": [{
    "name": "array",
    "type": 5,
    "type_size": 0,
    "dropped": false,
    "constraints": []
  }],
  "returning": [],
  "grants": []
}
select [1,2,3]::reverse;

col1
────
[-3, -2, -1]
# test: alter function rename
alter function reverse rename to reverse2;
show functions;

functions
─────────
{
  "user": "amelie",
  "name": "reverse2"
}
select show('state').lsn;

lsn
───
55
select [1,2,3]::reverse2;

col1
────
[-3, -2, -1]
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
select show('state').lsn;

lsn
───
55
show functions verbose;

functions
─────────
{
  "user": "amelie",
  "name": "reverse2",
  "text": "begin\n\tdeclare rev json := [];\n\tfor it in (array) do\n\t\trev := rev::push(-it::int);\n\tend;\n\treturn rev;\nend",
  "type": 5,
  "args": [{
    "name": "array",
    "type": 5,
    "type_size": 0,
    "dropped": false,
    "constraints": []
  }],
  "returning": [],
  "grants": []
}
select [1,2,3]::reverse2;

col1
────
[-3, -2, -1]
# test: drop function
drop function reverse2;
select show('state').lsn;

lsn
───
56
show functions verbose;

functions
─────────
# test: restart
disconnect S0;
close E0;
open E0;
connect E0 S0 127.0.0.1:3485;
# test: validate state
show functions verbose;

functions
─────────
select show('state').lsn;

lsn
───
56
disconnect S0;
close E0;
