#

open E0 "uuid": "00000000-0000-0000-0000-000000000000", "listen": [{ "host": "127.0.0.1", "port": 3485 }]
connect E0 S0 localhost:3485

# test: exists parsing
select exists
select exists 1
select exists (
select exists ()

# test: exists (expr)
select exists (1)
select exists (null)
select exists (false)
select exists ([])

# test: exists (set)
create shared table test (id int primary key, data int)
insert into test values (1, 0), (2, 0)
select exists (select * from test)
select exists (select * from test where false)

disconnect S0
close E0
