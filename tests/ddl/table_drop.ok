open E0 "uuid": "00000000-0000-0000-0000-000000000001", "listen": [{ "host": "127.0.0.1", "port": 3485 }];
connect E0 S0 127.0.0.1:3485;
# test: drop table statement parsing
drop;
error: drop❰;❱ ⟵ relation type expected
drop 1;
error: drop ❰1❱ ⟵ relation type expected
drop table "name";
error: drop table ❰"name"❱ ⟵ name expected
drop table name junk;
error: drop table name ❰junk❱ ⟵ ; expected
# test: drop non-existing table
drop table name;
error: table 'name': not exists
drop table if exists test;
# test: drop table
create table test(id int primary key);
select show('table', 'test', true).name;

name
────
"test"
drop table test;
select show('table', 'test', true).name;

name
────
null
# test: drop table if exists
create table test(id int primary key);
select show('table', 'test', true).name;

name
────
"test"
drop table test;
select show('table', 'test', true).name;

name
────
null
drop table if exists test;
# test: recreate same table after drop
create table test (id int primary key);
select show('table', 'test', true).name;

name
────
"test"
drop table test;
create table test (id int primary key);
select show('table', 'test', true).name;

name
────
"test"
drop table test;
# test: drop table with udf dep
create table test (id int primary key);
create function dep() begin insert into test values (123); end;
show functions verbose;

functions
─────────
{
  "user": "amelie",
  "name": "dep",
  "description": "",
  "text": "begin insert into test values (123); end",
  "type": 0,
  "args": [],
  "returning": [],
  "grants": []
}
drop table test;
error: table 'test': depends on function 'amelie.dep'
# test: drop table after udf drop
drop function dep;
drop table test;
# test: drop table with udf dep (cascade)
create table test (id int primary key);
create function dep() begin insert into test values (123); end;
show functions verbose;

functions
─────────
{
  "user": "amelie",
  "name": "dep",
  "description": "",
  "text": "begin insert into test values (123); end",
  "type": 0,
  "args": [],
  "returning": [],
  "grants": []
}
drop table test;
error: table 'test': depends on function 'amelie.dep'
drop table test cascade;
show tables;

tables
──────
show functions;

functions
─────────
# test: drop table (catalog hashtable collision)
create table d (id int primary key);
create table j (id int primary key);
insert into j values (1);
select * from j;

id
──
1
drop table d;
show tables;

tables
──────
{
  "user": "amelie",
  "name": "j",
  "description": ""
}
select * from j;

id
──
1
create table j (id int primary key, data int);
error: relation 'j': already exists
show tables;

tables
──────
{
  "user": "amelie",
  "name": "j",
  "description": ""
}
select * from j;

id
──
1
drop table j;
show tables;

tables
──────
disconnect S0;
close E0;
